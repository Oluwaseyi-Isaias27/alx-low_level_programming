#include <stdio.h>
#include <string.h>

/**
 * Main -Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
  char str1[55];

  strcpy(str1, "\"Programming is like building a multilingual puzzle");
